<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.easytriangle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PyTherom"
            android:label="PyTherom"
            android:parentActivityName=".ChooseActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.easytriangle.ChooseActivity"/>
        </activity>
        
        <activity android:name=".Angles"
            android:label="Angles"
            android:parentActivityName=".ChooseActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.easytriangle.ChooseActivity"/>
        </activity>

        <activity android:name=".Sin"
            android:label="Sin"
            android:parentActivityName=".ChooseActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.easytriangle.ChooseActivity"/>
        </activity>

        <activity android:name=".ChooseActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>